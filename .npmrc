# npm configuration for JetRoutes project

# Allow optional peer dependencies
legacy-peer-deps=true

# Use audit ignore for non-critical vulnerabilities
audit-level=moderate

# Prefer offline mode if packages are cached
prefer-offline=false

# Install exact versions
save-exact=false
